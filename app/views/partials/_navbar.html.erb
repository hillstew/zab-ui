<aside>
  <section class="aside-user-info">
    <h1><%= current_user.first_name %></h1>
    <%= image_tag "#{current_user.image}" %>
  </section>
  <%= button_to "Logout", logout_path, class: "btn btn-primary logout-btn", method: :delete if current_user %>
</aside>
